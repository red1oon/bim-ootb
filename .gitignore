*.db
!viewer/ad_seed.db
!erp/glassbowl_data.db
*.bin
node_modules/
test-results/
*.log

# OCI deploy staging (artifacts, not source) — §S281
oci-deploy/
# Safety: never commit OCI creds or the quick-push helper
*.pem
.oci/
quick-push.sh

# Reference context from HP machine (not source) — §S281
BIM_CONTEXT/
!erp/preview_demo.db
!buildings/warehouse_gardenworld.db

# Bonsai kernel: 22MB occt wasm is a vendored deploy asset, not git-tracked (slim build pending)
viewer/lib/kernel/occt-wasm.wasm
