*.db
!viewer/ad_seed.db
!viewer/mep_rw.db
!erp/glassbowl_data.db
# MODELLER PLAYGROUND — the Modeller's OWN building set on GH Pages, SEPARATE from the viewer.
# The viewer is never touched (it keeps fetching viewer/buildings → OCI). Only the modeller reads
# modeller/. Small SINGLE-file buildings only (<100MB, own meshes, no split companion). Large meshes
# (Terminal_geo = 261MB) can't live here: >100MB hard limit + GH Pages does NOT serve Git LFS → they
# stay on OCI, self-healed by db_resolve.js (W-DB-404-OCI-RETRY).
!modeller/SampleHouse_extracted.db
!modeller/Duplex_extracted.db
!modeller/SampleCastle_extracted.db
*.bin
node_modules/
test-results/
*.log

# OCI deploy staging (artifacts, not source) — §S281
oci-deploy/
# Safety: never commit OCI creds or the quick-push helper
*.pem
.oci/
quick-push.sh

# Reference context from HP machine (not source) — §S281
BIM_CONTEXT/
!erp/preview_demo.db
!buildings/warehouse_gardenworld.db

