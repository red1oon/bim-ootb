# ERP service-worker (`erp/sw.js`) version history

> Moved out of the `CACHE_VERSION` inline comment (2026-06-15) — it had grown to ~40 KB on one line and shipped to every browser.
> **Going forward the git commit message IS the changelog** (see `git log -- erp/sw.js`). This file holds the pre-extraction history only;
> do NOT re-append new entries to the `sw.js` comment.

---

- **v687**: BLUE FUTURE browser legs (FRONTEND_LANE_MASTER §OUTSTANDING item 0 — engine shipped v685). NEW blue_future.js (window.BlueFuture, W-BLUE-FUTURE-LIVE): the UNOFFICIAL speculative-branch SKIN over kernel v9. Leg 1 dot rail + long-press gestures (white anchor: long-press → enter blue, or discard-all when blue exists w/ confirm; blue dot: long-press → acceptBranchUpTo, tap → zoom children). Leg 2 PERVASIVE UNOFFICIAL treatment (SAFETY): blue rim on every window incl GL canvas + an UNOFFICIAL top banner + a @media print UNOFFICIAL watermark + BlueFuture.stampExport prefix. Leg 3 zoom into blue M_InOut/C_Invoice. Leg 4 (SAFETY): crud_overlay.js?v=9 read-sites tipDocs/listTip/readTip/tipValues/fieldLineage gain an optional branch arg + _branchClause — official chrome now filters branch_id IS NULL (blue was LEAKING via these raw SELECTs that bypass replayOps); the controller's readBranch() seam feeds the active branch only in blue mode. Leg 5: the EXISTING DocAction fan-out (commitGroup via _commitMeta()→{branch_id}) lands speculative — zero new fan-out code. idempiere.html wires blue_future.js?v=1 + bluefuture:changed/zoom refold.
- **v686**: RENDERER #2 — ODOO DESCRIPTOR (RENDERER2_ODOO.md increment 2; IDEMPIERE_2.md pivot "one renderer, N dictionaries"). NEW odoo_descriptor.js registers a SECOND ErpDescriptor ('odoo') whose 3 facets {structure,data,session} satisfy the SAME contract AD does, so ?erp=odoo drives the UNCHANGED idempiere.html chrome over an Odoo model with ZERO per-model chrome code (only a <script> tag + this precache changed). NON-INVENT: odoo_model.json (menus/windows/fields/records) + odoo_chain.json (folded O2C chain) are pulled LIVE from running Odoo 17 (odoodemo) by odoo_agent/extract_model.js + agent.js via JSON-RPC — selection of real rows, not a server. structure.init(db) INJECTS the Odoo slice as real tables into the chrome's sql.js db so the unchanged render path reads Odoo rows like AD rows; AD path untouched (odoo.init never runs unless ?erp=odoo). Witness tests/poc_odoo_descriptor.js W-ODOO-DESCRIPTOR (activeName=odoo, menu via getMenuTree, S00023 SO rows via getWindow+readRecords, 0 pageerrors) + AD regression poc_descriptor_seam.js 7/7. NEW precache odoo_descriptor.js + odoo_model.json + odoo_chain.json.
- **v685**: BLUE FUTURE engine + PER-FIELD LINEAGE hover (FRONTEND_LANE_MASTER §OUTSTANDING items 0+3b). kernel_ops.js?v=2 (v9: additive branch_id col ∉ _canonical + replayOps(db,type,branch) official-default + branchOps/discardBranch/acceptBranchUpTo — W-BLUE-FUTURE 9/9 headless). crud_overlay.js?v=8 (CORE.fieldLineage = always-on per-field value history folded from the op-log, kills AD_ChangeLog — W-FIELD-LINEAGE 6/6; + lineageHover ~900ms dwell blurb over [data-col]/[data-ad-column]/[data-ad-col]; __crud.fieldLineage). idempiere.html grid <td> gains data-ad-col. Engine headless-proven; browser leg = §LINEAGE-HOVER live probe.
- **v684**: SOUND-FX PILL on idempiere.html (UI_UX_LANE.md §FOLLOW-UP ROUND 2 item 3, user 2026-06-14 "why not use the pill audio icon in BIM") — the SAME speaker pill as the BIM viewer (panels.js id=audio, key v, volume2 glyph already in icons.js) surfaces the ERP/POS audio toggle instead of an invisible default. idempiere.html already loads ../viewer/sfx.js (window.toggleSfx + window.__sfx), so pills_idmp.json?v=31 +audio (order 8.7) + IdmpPillActions.audio (toggleSfx → builder.sync() re-lights on the spot) + IdmpPillActive.audio (→ __sfx.isOn()). The choice PERSISTS (localStorage sfx_on overrides erp/sfx.json master.enabled) so a mute sticks across reloads. idmp_pills.js?v=12 (manifest fetch ?v=30→31). Presentation-only, newVerbs=[], Lucide-only ([[feedback_pill_icon_consistency]]). Witness erp/tests/poc_idmp_pills.js (§AUDIO-PILL: renders, tap toggles §SFX_TOGGLE + __sfx.isOn flips + lit).
- **v683**: AD SELF-EDIT LIVE (W-AD-SELFEDIT-LIVE, FRONTEND_LANE_MASTER §PENDING WITNESS) — a SIGNED dictionary edit repaints the form on the spot, no reload. ad_parser.js?v=23 gains setTipSource: its AD_Field/AD_Window reads OVERLAY the signed sidecar edit via CrudOverlay.listTip(window.__crud.kernelDb(),…) (same verb as engine witness W-AD-SELFEDIT). crud_overlay.js?v=7 emits an overlay:committed event after a sealed CRUD commit. idempiere.html wires the tip-source + a refold hook: on an AD_* commit it invalidates _openWins[winId] + re-openWindow/buildMenu → re-read-the-dictionary, not recompile. Live witness §W-AD-SELFEDIT-LIVE PASS (headless chrome: M_MatchInv "Organization" grid column vanishes on IsDisplayed Y→N, returns on N→Y; commit sealed+verifyChain ok). Engine regressions W-AD-SELFEDIT/OPLOG-DISTRIB/NINJA-EXPORT/EXTRACT green.
- **v682**: DESCRIPTOR SEAM (renderer #2 increment 1; IDEMPIERE_2.md pivot, IDEMPIERE_RENDERER_SPEC.md descriptor-seam). idempiere.html's chrome now reads its dictionary/data/session from ErpDescriptor.active (3 facets: structure/data/session) instead of hardcoded window.ADParser/ADData/IdmpSession. AD is the FIRST descriptor — its facets ARE those AD globals verbatim, so behavior is IDENTICAL (the 24 call sites unchanged; ADP/ADD/SES alias the active descriptor). ?erp=<name> + ErpDescriptor.use() repoint the whole chrome at a 2nd model (Odoo, increment 2). NEW erp_descriptor.js (precached). Witnesses: tests/poc_descriptor_seam.js W-DESCRIPTOR-SEAM 7/7 (registered=ad, facets===AD globals identity, login->menu 294/332->window opens->12 records via the seam, 0 pageerrors); regressions test_idempiere_{fold,login,master_detail}+poc_idmp_host green. PRIOR --
- **v680**: POS FOLLOW-UP ROUND 2 (UI_UX_LANE.md §FOLLOW-UP ROUND 2 B/D-R2 + R2-AUDIO, user live-test 2026-06-14, presentation-only newVerbs=[]) — pos_lens.js?v=9 + icons.js?v=10 + ../viewer/sfx.js on idempiere.html: §R2-1 top bar shows the live RUNNING TOTAL (was a blank cart-icon bar) · §R2-2 scan-QR moved to the RIGHT of that top bar · §R2-3 the rim pay-panel RETIRED (orange/green edge drawers + receipt-preview modal gone) → ONE single Pay icon on the right (#pos-float-tender completes directly) · §R2-4 the pay panel is DRAGGABLE (grab-header, ported _makeDraggable idiom, §POS-FLOAT-DRAG) · §R2-5 BUG FIX: the empty-partner gate silently blocked the old banknote ("Pay stopped working") → the partner is DEFAULTED (cashtrx else first-active, §POS-PARTNER-DEFAULT) so the single Pay icon completes out of the box (buildSaleGroup→commitGroup→receipt, §POS-CENT byte-identical) · §R2-6 dock glyphs: import→(+) plus, deliver-later→route (the WH-walk icon, not an invented one; still opens the walk in a new tab) · §R2-AUDIO subtle POS earcons (pos_ring/pos_pay/pos_complete) via the SAME viewer synth engine reading erp/sfx.json (enabled, ui_clicks:false → no per-button spam; guarded no-op if absent, §POS-AUDIO). Witnesses W-POS-LIVE (§POS-CENT maxDiff=0c) + W-WH-POS-PICK-LIVE green; sfx engine loads on the ERP page (§SFX_INIT enabled=true), earcons §POS-AUDIO sfx=played.
- **v679**: ERP AUDIT CHANGELOG (ERP_AUDIT_CHANGELOG.md Tasks 0-4 + iDempiere-convention polish). Task 0 applySession() wires window.APP.actor/clientId/orgId. Task 1 buildOp CRUD_CREATE carries stdDefaults{actor,clientId,orgId}, CRUD_UPDATE carries actor; listTip materialises CreatedBy/UpdatedBy/Created/Updated/AD_Client_ID/AD_Org_ID/IsActive/Processed/Processing/Posted (PRAGMA-guarded via global.__idmpDb); _fmtKernelTs renders the kernel epoch-ms ts into iDempiere yyyy-MM-dd HH:mm:ss so created rows match the seed format. Task 2 CORE.changeLog AD-filtered (AD_Table.IsChangeLog + AD_Column.IsAllowLogging) + clock-Log toolbar overlay resolving actor->AD_User.Name via window.UserNames + fmtTs. Task 3 delete-tenant honest op-log count _countUserOpsForClient. Task 4 _allocDocNo from AD_Sequence DocumentNo_<Table>. NEW precache user_names.js, crud_overlay.js v4->6, icons.js v9. eslint.globals.json +Materialize (registers the viewer P5 UMD global -> unblocks no-undef gate). Witnesses scripts/poc_audit_changelog.js 25/25 + tests/poc_audit_changelog_live.js 6/6; regressions W-CRUD-LIST/GATE/PERSIST/GROUP/DOCSTATUS green.
- **v678**: NINJA MODE tribute — plugin_overlay.js?v=3 Create tab credits Red1 Ninja: subtitle "Ninja mode — your spreadsheet becomes a running module" + byline "A friendlier Red1 Ninja, in the browser — Excel defines the model; no JVM, no 2Pack". Presentation-only (additive markup + .pe-ninja/.pe-byline CSS); Create flow/§NINJA-DOM-WITNESS unchanged. Doc docs/ERPUserGuide.md §9 reframed (Ninja mode leads, PackOut/PackIn = analogy).
- **v677**: W drawer DYNAMIC perpendicular (all surfaces) — idmp_pills.js + glassbowl_pills.js measure the pill strip (_host.getBoundingClientRect height>=width) and draw the W long-press drawer ACROSS it: VERTICAL strip → row to the LEFT of W, HORIZONTAL strip → column ABOVE. Replaces v676's hardcoded idmp row (brittle if a bar's orientation flips — the regression that bit idmp when it went vertical). Witness erp/tests/poc_wdrawer_perp.js (W-PERP): idmp vertical flexDir=row overlaps=0 + glassbowl horizontal flexDir=column overlaps=0 (real getBoundingClientRect).
- **v676**: W drawer PERPENDICULAR on idempiere.html — idmp_pills.js _worldDrawer was an UPWARD column (flex-direction:column, bottom-anchored) that ran PARALLEL to the now-VERTICAL right-edge pill strip → it COVERED the pills stacked above W. Now a SIDEWAYS row to the LEFT of the W pill (flex-direction:row, top:r.top, right:innerWidth-r.left+6), mirroring the viewer fix (panels.js _worldHistDrawer, sw v651). bomb leftmost (safety/far), Z rightmost (adjacent).
- **v675**: POS dispose + WH icon fix — pos_lens.js?v=8 homeBtn now removes floatPanel+payModal+dock from body (§POS-HOME dispose=float+modal+dock); icons.js?v=9 +route (Lucide verbatim, SPATIAL_PICKING_SPEC §S-3); pills_idmp.json?v=30 warehouse icon box→route (was missing, caused dark pill); wh_walk.js?v=6 #wh-route-toggle in strip (WH mode indicator + route-drawer toggle, merged — the route icon IS the drawer; strip no longer has a separate route-drawer header). idmp_pills.js fetch pills_idmp.json?v=29→30.
- **v674**: IS blank fix (R-T4) — ad_seed.db +pa_reportcolumnset(100/101) +pa_reportcolumn(12 rows, BS+IS sets, from iDempiere Postgres) → IS/BS now have cols>0 on idempiere.html; §STATEMENT report=101 cols=6 cells=222 §R-T4-verdict=PASS. Rogue pills: #reportMenuPill + #crudModeWrap fixed floats removed — Reports+EditMode now registry pills (pills_idmp.json ids=reports/editmode, IdmpPillActions wired, window.__crud.toggleEditMode exposed). Burger button-in-a-button unnested.
- **v673**: NINJA CREATE (PackOut) face on the Plugin Engine pill (NINJA_MODE_PILL.md) — plugin_overlay.js?v=2 gains a ( Install )( Create ) tab toggle: drop an .xlsx model sheet → NinjaCreate.previewSheet derives AD tables (name:refType chips, ↳ detail-of, warnings) → Emit & Install runs the bundle's activate→stageModels through the WRITABLE page sql.js handle (NinjaBundle.makeWritableDbHost(window.__idmpDb); §5 decision: AD db == kernel db == one writable handle that NinjaPill already writes through) → the new bundle shows ACTIVE in the Install list. Download-starter generates ninja_starter.xlsx client-side (NinjaStarter.starterBlob — no shipped fixture). NEW precached engine modules ninja_{model,stage,bundle,create,starter}.js (== bim-compiler build/erp source, headless-proven W-NINJA-PILL / poc_ninja_create 🟢: starter→2 windows, Ninja_HRMIS→18 windows, re-emit idempotent) loaded model→stage→bundle→create→starter BEFORE plugin_overlay (bundle/create capture deps at module-load); icons.js?v=8 +upload (verbatim Lucide). DISTINCT from the NINJA EXCEL reporting lens (ninja_excel/rule/pill) — they share SheetJS only. §-witness §NINJA-PILL-DOM tab/drop/starter/emit.
- **v672**: SUBSTRATE WIRING 1/5 — vfs_detect.js wired live into erp.html + idempiere.html boot (CONSTRAINT_MITIGATION_LANE item 3): §VFS chosen=opfs|idb logged at boot (FoldEngineConstraints §6 vfs_backend monitor), result on window.__VFS. GH Pages = IDB (no COOP/COEP) — NAMED, no silent downgrade. Additive: no data-path change. Witness tests/poc_vfs_wire.js (§VFS at boot, idb on this origin).
- **v671**: INIT-BUBBLE JUMP FIX (ad_ui.js?v=29) — home globe auto-maximize now SYNCHRONOUS (was setTimeout(_resizeGraph(true),100)) so the constellation paints at final viewport size on the first frame; no paint-small(360h)-then-grow(600h) JUMP on load/refresh. NOT a reburst: initFromBubbles fires once/load (resize-in-place, no destroy/reinit). Witness tests/poc_init_reburst.js: resizedAfterPaint Y→N, burst=1, first paint 194ms (≤300).
- **v670**: PLUGIN ENGINE PILL (PLUGIN_SYSTEM_LANE.md §Phase D, W-PLUGIN) — the Fold-Engine plugin host lands on idempiere.html: NEW plugin_registry.js (window.PluginRegistry — OSGi-like install/start/stop/uninstall lifecycle + semver-checked topological resolveDeps; UMD == bim-compiler build/erp source, headless-proven W-PLUGIN) + plugin_overlay.js (window.PluginEngine — the 'plug' pill overlay: paste a raw ES-module bundle URL → one click → ACTIVE; collapses iDempiere's drop-JAR-restart + 2Pack-import into one in-surface action; bundle state {id,url,version,state} persists in IndexedDB 'fold_plugins', re-activates on open) + ad_callout.js (window.AdCallout, gives callout bundles a live target; joins ad_modelval/ad_process/post_resolver). A bundle contributes into the live engine registries via activate(ctx); PLUGIN_* ops land in kernel_ops (Phase B). pills_idmp.json +plugin (plug glyph, order 8.5); idempiere.html wires the 3 scripts + IdmpPillActions.plugin. Three example bundles precached under plugins/ (C-1 callout uppercases M_Product.Name, C-2 validator rejects M_Production qty<0, C-3 token resolves {Production.WIP}=50005 from seed). Witness scripts/poc_plugin.js exit 0 — §PLUGIN-INSTALL/START + §PLUGIN-CALLOUT name=WIDGET + §PLUGIN-VALRULE M_Production qty<0 REJECT + §PLUGIN-TOKEN WIP=50005, cycle+semver-conflict falsifiers fire, kernel_ops audit 3 install/3 start/1 stop/1 uninstall. PRIOR —
- **v669**: REPORTING ENGINE → bim-ootb (R-T2 default fall-through, REPORTING_UI_DEFAULT_FALLTHROUGH.md). The full canonical report_overlay.js (build/erp source-of-truth, 256→966 lines) ships: REPORTING_LANE engine (resolveScope/periodsOf/statementInputs/renderStatement) + the R-T2 default fall-through — an UNCONFIGURED report now lands on populated Fact_Acct history (latest posted period in the fact-bearing year, period 160 / 2002-03 band) with a coverage:default→history banner instead of a vacuous matrix; folds byte-identical. SUPERSEDES the stale PR #253 (its engine is absorbed; #253's older periodModel/windowFor was refactored to resolveScope/periodsOf and left open for fold-back review). report_overlay.js?v=1→2 (idempiere.html); glassbowl.html loads it unversioned (this CACHE_VERSION bump purges it). VERIFIED on the canonical: scripts/poc_report_{open,default_fallthrough}.js (R-T1/R-T2) exit 0 — §REPORT-DEFAULT period=160 facts=282 nonzeroCells=42 coverage=default→history verdict=PASS, TB totalDr=totalCr=46574.97 maxDiff=0c, absent-data→coverage=absent falsifier fires. PRIOR —
- **v668**: POS MINIMALIST (UI_UX_LANE Track D) — pos_lens.js?v=7: §D-1 orange/green edge-rim drawers (fully textless, tap-to-expand, title= hover only); §D-2 total flanked by scan (left, add item) + banknote (right → receipt-preview modal #pos-pay-modal: [QR demo][#pos-pay-ok=manual-pay→Complete][Cancel]); §D-3 ⋯ dock (home/import/receipt/deliver-later, reveal-up, outside-click-closes, deliver-later still dictionary-gated); §D-4 cart-pill #pos-pill-payment toggle open/close (§POS-FLOAT toggle=); §D-5 deliver-later POC opens WH walk in new tab (§POS-DELIVERLATER walk-tab=opened); §D-6 W-POS-LIVE + W-WH-POS-PICK-LIVE green (witnesses tap #pos-float-tender→#pos-pay-ok). Engine/op-group byte-identical, newVerbs=[], Lucide-only, presentation-only. PRIOR —
- **v667**: SO FULL CRUD (SO_FULL_CRUD_GAP.md T1-T4) — the CRUD ring-of-fire overlay now mounts on idempiere.html too (T3, host-anchored, no fork): toolbar '✎ CRUD' opens the ring on the FOCUSED record; create/update/delete + the T1 Complete DOCUMENT PROCESS (M_InOut ship + C_Invoice + SET_STATUS as ONE sealed group, GL coverage-gated) all ride the shared overlay. Glassbowl renderDataTab now UNIONs CRUD_CREATE rows + hides CRUD_DELETE tombstones from the signed sidecar (T2, CORE.listTip); owner-gate/CAS REJECT enforced pre-seal (T4). Precache adds crud_overlay.js?v=4 + crud_ops.json (idempiere now references the overlay); erp_engine.js already precached. Witnesses W-SO-COMPLETE-UI / W-CRUD-LIST / W-CRUD-GATE headless-green.
- **v664**: NINJA sample fetch-and-inform — the Excel Report lens's sample link is now a handler (ninja_pill.js?v=3, NinjaPill.fetchSample): LOCAL copy first (same-origin asset, SW-precached), falls back to the canonical GH Pages URL when no local copy is reachable, and TOASTS the user what happened + what to do next ('open in Excel / drop it back here'); offline-and-uncached → honest 'connect once and retry' toast, §NINJA-PILL sample source=local|gh-pages|UNREACHABLE. W-NINJA-PILL step 6 (local hit + hermetic GH fallback via fetch stub — Playwright routes can't see SW-precache answers).
- **v663**: POS DELIVER-LATER DOOR (WH_POS_PICK_LANE W-1, SPATIAL_PICKING_SPEC §S-2b) — pos_lens.js?v=5 payment panel gains 'Deliver later · pick at warehouse' (#pos-float-deliverlater), DICTIONARY-GATED on a docsubtypeso='SO' doctype in the loaded db (seed 132); rides POSCore.buildDeliverLaterGroup VERBATIM (W-POS-DELIVERLATER: order CO + shipment born DR, invoice timing named from C_Order.InvoiceRule) then PERSISTS the op log via the new host persist (idempiere.html → KanbanHost.persist(opDb, idmp_kanban_proj)) so the warehouse walk can fold the sale — the §S-2b seam. pos_core.js?v=3 synced to the bim-compiler engine (gains deliverLaterPolicy/buildDeliverLaterGroup/completeShipmentOps); NEW erp/inout_confirm.js (W-WH-CONFIRM fold, lazy-fetched by the viewer walk, not precached). Tender path byte-identical (W-POS-LIVE falsifier). §POS-DELIVERLATER door=on/off + sale lines. PRIOR —
- **v660**: POS float-panel dispose-with-cart (§POS-FLOAT dispose=cart-empty — sale complete / last line removed closes the panel; ✕ + swipe-down landed v658; pos_lens ?v=4) + viewer PWA-resume dead-link recovery — OCI-era pwa_last_db bricked bare viewer opens; clears the key, ONE redirect to the landing (viewer sw v647, main.js?v=41).
- **v659**: NINJA EXCEL pill (NinjaExcelAdaptation.md §6.2, W-NINJA-PILL) — 'Excel Report' lens on idempiere.html: NEW ninja_excel.js (engine, == bim-compiler build/erp, headless-proven W-NINJA/W-NINJA-MAKE/W-NINJA-RULE maxDiff=0c) + ninja_pill.js (drop/download UI; MAKE scaffolds from a filled BACKUP sample, RUN folds Process-sheet SQL over the loaded tenant db via sql.js + verify-by-example in integer cents) + xlsx.mini.min.js (SheetJS, vendored, LAZY-loaded on first open) + ninja_sample.xlsx (GardenWorld invoice summary generated from ad_seed.db STORED columns — runnable as-is). pills_idmp.json +ninja (grid glyph, order 4.8); idempiere.html wires scripts + IdmpPillActions.ninja.
- **v658**: CONSISTENCY FINISH (prompts/CONSISTENCY_FINISH.md §K-1..§K-4, the 2026-06-12 cross-lane review's finish items) — NEW precached overlay_kit.js (window.OverlayKit: el/ensureSql/ico — the utilities erp_picker.js + migrate_showme.js each carried byte-identical, plus the ONE Lucide icon renderer over icons.js; ensureSql reuses the page's window.SQL, one WASM init per page); erp_picker.js?v=28/3 + migrate_showme.js?v=23 consume the kit and drop EVERY unicode glyph (brand 🟧🟣🔵🔴🟦→CSS color dots, ⬇→download, 🔌→plug, 🔒→lock, ✓/✗→check/xmark, ⧉→clipboard, &larr;/→→chevronLeft/next, ●→CSS livedot, step-done ::after ✓→data-URI check) per feedback_pill_icon_consistency; icons.js?v=26/5 +check/xmark/lock/plug/chevronLeft (verbatim Lucide, circleHelp precedent; idempiere.html loads icons+kit BEFORE migrate_showme — its STEPS render icons at module load). §K-3: installShard()'s persisted success-path records a World-history dot (WholeHistory.record page=idempiere kind=op 'Installed <shard> (N rows, clients a→b)') — the largest user state change was the only one leaving no W trace; guarded, NOT a signed op (install=infrastructure, outside the Z fold lane). §K-4b crud_overlay.js?v=3: crudFoldBack/crudFoldForward now RETURN Promise<boolean> (ok=Y/N appended to §FOLD-BACK/§FOLD-FORWARD; sidecar is async so a sync bool would be invented). §K-4c idmp_history.js?v=8: warn-once §IDMP-HIST no-restoreFn when the host never registerRestore()d (was: silent display-only bar). Witnesses: W-OVERLAY-KIT (poc_overlay_kit.js: kit consumed, zero glyphs in rendered ep-/ms- DOM) + W-INSTALL-DOT (poc_install_histdot.js: dialog install → §WHOLE-REC) + regressions poc_install_persist/poc_install_idmp/poc_client_switcher green.
- **v655**: POS FULL LOOP §L-1..§L-3 (POS_FULL_LOOP.md DONE) — replenish-PO-on-tap wiring in pos_lens.js: vendorOf() from m_product_po.iscurrentvendor=Y (honest refusal when absent), tap → buildReplenishPO → commitGroup (DR state); §POS-REPLENISH-PO log on tap; W-POS-CRUD/W-POS-VOID/W-POS-REPLENISH-LOOP all green headless. newVerbs=[]. Loop closed: suggest→PO CO→receipt CO→onhand +11→suggestion clears.
- **v657**: G-2 sha256 imageKey (POS_GAP_CLOSE) — pos_lens Import photo key = 'sha256:'+hex(SubtleCrypto digest of the DECODED bytes), replacing the content-length stub (live wiring now matches the W-IMG-SYNC content-addressing proof); img_store.resolveImage gains digestOf verification (tampered folder blob → §IMG-TAMPER, thumb keeps rendering); read path in _addNewTileCard rides resolveImage. Witness W-IMG-LIVE (poc_img_live.js): live-page IDB put/get round-trip + tier walk full→thumb→none + tamper falsifier. ?v= bumps: img_store 1→2, pos_lens 2→3.
- **v655**: POS FULL LOOP §L-1..§L-3 (POS_FULL_LOOP.md DONE) — replenish-PO-on-tap wiring in pos_lens.js: vendorOf() from m_product_po.iscurrentvendor=Y (honest refusal when absent), tap → buildReplenishPO → commitGroup (DR state); §POS-REPLENISH-PO log on tap; W-POS-CRUD/W-POS-VOID/W-POS-REPLENISH-LOOP all green headless. newVerbs=[]. Loop closed: suggest→PO CO→receipt CO→onhand +11→suggestion clears. PRIOR (origin/main) --
- **v681**:
- **v681**: NINJA EXPORT (PackOut existing window) — NINJA_MODE_LANE.md §1. ninja_stage.js?v=2 now carries extractModel (reverse-export, W-NINJA-EXTRACT) + master-FK-detect fix (LAST *_ID col); new ninja_export.js (window.NinjaExport, W-NINJA-EXPORT roundtrip=MATCH 21/21 tables, extractModel→workbook serialize) precached; plugin_overlay.js?v=4 Create face gains an "Export an existing window" picker (live AD db <select> → exportBlob → .xlsx download). Live DOM smoke §W-NINJA-EXPORT-LIVE PASS (AST_Asset.xlsx exact round-trip in headless chrome).
- **v680**: POS FOLLOW-UP ROUND 2 (UI_UX_LANE.md §FOLLOW-UP ROUND 2 B/D-R2 + R2-AUDIO, user live-test 2026-06-14, presentation-only newVerbs=[]) — pos_lens.js?v=9 + icons.js?v=10 + ../viewer/sfx.js on idempiere.html: §R2-1 top bar shows the live RUNNING TOTAL (was a blank cart-icon bar) · §R2-2 scan-QR moved to the RIGHT of that top bar · §R2-3 the rim pay-panel RETIRED (orange/green edge drawers + receipt-preview modal gone) → ONE single Pay icon on the right (#pos-float-tender completes directly) · §R2-4 the pay panel is DRAGGABLE (grab-header, ported _makeDraggable idiom, §POS-FLOAT-DRAG) · §R2-5 BUG FIX: the empty-partner gate silently blocked the old banknote ("Pay stopped working") → the partner is DEFAULTED (cashtrx else first-active, §POS-PARTNER-DEFAULT) so the single Pay icon completes out of the box (buildSaleGroup→commitGroup→receipt, §POS-CENT byte-identical) · §R2-6 dock glyphs: import→(+) plus, deliver-later→route (the WH-walk icon, not an invented one; still opens the walk in a new tab) · §R2-AUDIO subtle POS earcons (pos_ring/pos_pay/pos_complete) via the SAME viewer synth engine reading erp/sfx.json (enabled, ui_clicks:false → no per-button spam; guarded no-op if absent, §POS-AUDIO). Witnesses W-POS-LIVE (§POS-CENT maxDiff=0c) + W-WH-POS-PICK-LIVE green; sfx engine loads on the ERP page (§SFX_INIT enabled=true), earcons §POS-AUDIO sfx=played.
- **v679**: ERP AUDIT CHANGELOG (ERP_AUDIT_CHANGELOG.md Tasks 0-4 + iDempiere-convention polish). Task 0 applySession() wires window.APP.actor/clientId/orgId. Task 1 buildOp CRUD_CREATE carries stdDefaults{actor,clientId,orgId}, CRUD_UPDATE carries actor; listTip materialises CreatedBy/UpdatedBy/Created/Updated/AD_Client_ID/AD_Org_ID/IsActive/Processed/Processing/Posted (PRAGMA-guarded via global.__idmpDb); _fmtKernelTs renders the kernel epoch-ms ts into iDempiere yyyy-MM-dd HH:mm:ss so created rows match the seed format. Task 2 CORE.changeLog AD-filtered (AD_Table.IsChangeLog + AD_Column.IsAllowLogging) + clock-Log toolbar overlay resolving actor->AD_User.Name via window.UserNames + fmtTs. Task 3 delete-tenant honest op-log count _countUserOpsForClient. Task 4 _allocDocNo from AD_Sequence DocumentNo_<Table>. NEW precache user_names.js, crud_overlay.js v4->6, icons.js v9. eslint.globals.json +Materialize (registers the viewer P5 UMD global -> unblocks no-undef gate). Witnesses scripts/poc_audit_changelog.js 25/25 + tests/poc_audit_changelog_live.js 6/6; regressions W-CRUD-LIST/GATE/PERSIST/GROUP/DOCSTATUS green.
- **v678**: NINJA MODE tribute — plugin_overlay.js?v=3 Create tab credits Red1 Ninja: subtitle "Ninja mode — your spreadsheet becomes a running module" + byline "A friendlier Red1 Ninja, in the browser — Excel defines the model; no JVM, no 2Pack". Presentation-only (additive markup + .pe-ninja/.pe-byline CSS); Create flow/§NINJA-DOM-WITNESS unchanged. Doc docs/ERPUserGuide.md §9 reframed (Ninja mode leads, PackOut/PackIn = analogy).
- **v677**: W drawer DYNAMIC perpendicular (all surfaces) — idmp_pills.js + glassbowl_pills.js measure the pill strip (_host.getBoundingClientRect height>=width) and draw the W long-press drawer ACROSS it: VERTICAL strip → row to the LEFT of W, HORIZONTAL strip → column ABOVE. Replaces v676's hardcoded idmp row (brittle if a bar's orientation flips — the regression that bit idmp when it went vertical). Witness erp/tests/poc_wdrawer_perp.js (W-PERP): idmp vertical flexDir=row overlaps=0 + glassbowl horizontal flexDir=column overlaps=0 (real getBoundingClientRect).
- **v676**: W drawer PERPENDICULAR on idempiere.html — idmp_pills.js _worldDrawer was an UPWARD column (flex-direction:column, bottom-anchored) that ran PARALLEL to the now-VERTICAL right-edge pill strip → it COVERED the pills stacked above W. Now a SIDEWAYS row to the LEFT of the W pill (flex-direction:row, top:r.top, right:innerWidth-r.left+6), mirroring the viewer fix (panels.js _worldHistDrawer, sw v651). bomb leftmost (safety/far), Z rightmost (adjacent).
- **v675**: POS dispose + WH icon fix — pos_lens.js?v=8 homeBtn now removes floatPanel+payModal+dock from body (§POS-HOME dispose=float+modal+dock); icons.js?v=9 +route (Lucide verbatim, SPATIAL_PICKING_SPEC §S-3); pills_idmp.json?v=30 warehouse icon box→route (was missing, caused dark pill); wh_walk.js?v=6 #wh-route-toggle in strip (WH mode indicator + route-drawer toggle, merged — the route icon IS the drawer; strip no longer has a separate route-drawer header). idmp_pills.js fetch pills_idmp.json?v=29→30.
- **v674**: IS blank fix (R-T4) — ad_seed.db +pa_reportcolumnset(100/101) +pa_reportcolumn(12 rows, BS+IS sets, from iDempiere Postgres) → IS/BS now have cols>0 on idempiere.html; §STATEMENT report=101 cols=6 cells=222 §R-T4-verdict=PASS. Rogue pills: #reportMenuPill + #crudModeWrap fixed floats removed — Reports+EditMode now registry pills (pills_idmp.json ids=reports/editmode, IdmpPillActions wired, window.__crud.toggleEditMode exposed). Burger button-in-a-button unnested.
- **v673**: NINJA CREATE (PackOut) face on the Plugin Engine pill (NINJA_MODE_PILL.md) — plugin_overlay.js?v=2 gains a ( Install )( Create ) tab toggle: drop an .xlsx model sheet → NinjaCreate.previewSheet derives AD tables (name:refType chips, ↳ detail-of, warnings) → Emit & Install runs the bundle's activate→stageModels through the WRITABLE page sql.js handle (NinjaBundle.makeWritableDbHost(window.__idmpDb); §5 decision: AD db == kernel db == one writable handle that NinjaPill already writes through) → the new bundle shows ACTIVE in the Install list. Download-starter generates ninja_starter.xlsx client-side (NinjaStarter.starterBlob — no shipped fixture). NEW precached engine modules ninja_{model,stage,bundle,create,starter}.js (== bim-compiler build/erp source, headless-proven W-NINJA-PILL / poc_ninja_create 🟢: starter→2 windows, Ninja_HRMIS→18 windows, re-emit idempotent) loaded model→stage→bundle→create→starter BEFORE plugin_overlay (bundle/create capture deps at module-load); icons.js?v=8 +upload (verbatim Lucide). DISTINCT from the NINJA EXCEL reporting lens (ninja_excel/rule/pill) — they share SheetJS only. §-witness §NINJA-PILL-DOM tab/drop/starter/emit.
- **v672**: SUBSTRATE WIRING 1/5 — vfs_detect.js wired live into erp.html + idempiere.html boot (CONSTRAINT_MITIGATION_LANE item 3): §VFS chosen=opfs|idb logged at boot (FoldEngineConstraints §6 vfs_backend monitor), result on window.__VFS. GH Pages = IDB (no COOP/COEP) — NAMED, no silent downgrade. Additive: no data-path change. Witness tests/poc_vfs_wire.js (§VFS at boot, idb on this origin).
- **v671**: INIT-BUBBLE JUMP FIX (ad_ui.js?v=29) — home globe auto-maximize now SYNCHRONOUS (was setTimeout(_resizeGraph(true),100)) so the constellation paints at final viewport size on the first frame; no paint-small(360h)-then-grow(600h) JUMP on load/refresh. NOT a reburst: initFromBubbles fires once/load (resize-in-place, no destroy/reinit). Witness tests/poc_init_reburst.js: resizedAfterPaint Y→N, burst=1, first paint 194ms (≤300).
- **v670**: PLUGIN ENGINE PILL (PLUGIN_SYSTEM_LANE.md §Phase D, W-PLUGIN) — the Fold-Engine plugin host lands on idempiere.html: NEW plugin_registry.js (window.PluginRegistry — OSGi-like install/start/stop/uninstall lifecycle + semver-checked topological resolveDeps; UMD == bim-compiler build/erp source, headless-proven W-PLUGIN) + plugin_overlay.js (window.PluginEngine — the 'plug' pill overlay: paste a raw ES-module bundle URL → one click → ACTIVE; collapses iDempiere's drop-JAR-restart + 2Pack-import into one in-surface action; bundle state {id,url,version,state} persists in IndexedDB 'fold_plugins', re-activates on open) + ad_callout.js (window.AdCallout, gives callout bundles a live target; joins ad_modelval/ad_process/post_resolver). A bundle contributes into the live engine registries via activate(ctx); PLUGIN_* ops land in kernel_ops (Phase B). pills_idmp.json +plugin (plug glyph, order 8.5); idempiere.html wires the 3 scripts + IdmpPillActions.plugin. Three example bundles precached under plugins/ (C-1 callout uppercases M_Product.Name, C-2 validator rejects M_Production qty<0, C-3 token resolves {Production.WIP}=50005 from seed). Witness scripts/poc_plugin.js exit 0 — §PLUGIN-INSTALL/START + §PLUGIN-CALLOUT name=WIDGET + §PLUGIN-VALRULE M_Production qty<0 REJECT + §PLUGIN-TOKEN WIP=50005, cycle+semver-conflict falsifiers fire, kernel_ops audit 3 install/3 start/1 stop/1 uninstall. PRIOR —
- **v669**: REPORTING ENGINE → bim-ootb (R-T2 default fall-through, REPORTING_UI_DEFAULT_FALLTHROUGH.md). The full canonical report_overlay.js (build/erp source-of-truth, 256→966 lines) ships: REPORTING_LANE engine (resolveScope/periodsOf/statementInputs/renderStatement) + the R-T2 default fall-through — an UNCONFIGURED report now lands on populated Fact_Acct history (latest posted period in the fact-bearing year, period 160 / 2002-03 band) with a coverage:default→history banner instead of a vacuous matrix; folds byte-identical. SUPERSEDES the stale PR #253 (its engine is absorbed; #253's older periodModel/windowFor was refactored to resolveScope/periodsOf and left open for fold-back review). report_overlay.js?v=1→2 (idempiere.html); glassbowl.html loads it unversioned (this CACHE_VERSION bump purges it). VERIFIED on the canonical: scripts/poc_report_{open,default_fallthrough}.js (R-T1/R-T2) exit 0 — §REPORT-DEFAULT period=160 facts=282 nonzeroCells=42 coverage=default→history verdict=PASS, TB totalDr=totalCr=46574.97 maxDiff=0c, absent-data→coverage=absent falsifier fires. PRIOR —
- **v668**: POS MINIMALIST (UI_UX_LANE Track D) — pos_lens.js?v=7: §D-1 orange/green edge-rim drawers (fully textless, tap-to-expand, title= hover only); §D-2 total flanked by scan (left, add item) + banknote (right → receipt-preview modal #pos-pay-modal: [QR demo][#pos-pay-ok=manual-pay→Complete][Cancel]); §D-3 ⋯ dock (home/import/receipt/deliver-later, reveal-up, outside-click-closes, deliver-later still dictionary-gated); §D-4 cart-pill #pos-pill-payment toggle open/close (§POS-FLOAT toggle=); §D-5 deliver-later POC opens WH walk in new tab (§POS-DELIVERLATER walk-tab=opened); §D-6 W-POS-LIVE + W-WH-POS-PICK-LIVE green (witnesses tap #pos-float-tender→#pos-pay-ok). Engine/op-group byte-identical, newVerbs=[], Lucide-only, presentation-only. PRIOR —
- **v667**: SO FULL CRUD (SO_FULL_CRUD_GAP.md T1-T4) — the CRUD ring-of-fire overlay now mounts on idempiere.html too (T3, host-anchored, no fork): toolbar '✎ CRUD' opens the ring on the FOCUSED record; create/update/delete + the T1 Complete DOCUMENT PROCESS (M_InOut ship + C_Invoice + SET_STATUS as ONE sealed group, GL coverage-gated) all ride the shared overlay. Glassbowl renderDataTab now UNIONs CRUD_CREATE rows + hides CRUD_DELETE tombstones from the signed sidecar (T2, CORE.listTip); owner-gate/CAS REJECT enforced pre-seal (T4). Precache adds crud_overlay.js?v=4 + crud_ops.json (idempiere now references the overlay); erp_engine.js already precached. Witnesses W-SO-COMPLETE-UI / W-CRUD-LIST / W-CRUD-GATE headless-green.
- **v664**: NINJA sample fetch-and-inform — the Excel Report lens's sample link is now a handler (ninja_pill.js?v=3, NinjaPill.fetchSample): LOCAL copy first (same-origin asset, SW-precached), falls back to the canonical GH Pages URL when no local copy is reachable, and TOASTS the user what happened + what to do next ('open in Excel / drop it back here'); offline-and-uncached → honest 'connect once and retry' toast, §NINJA-PILL sample source=local|gh-pages|UNREACHABLE. W-NINJA-PILL step 6 (local hit + hermetic GH fallback via fetch stub — Playwright routes can't see SW-precache answers).
- **v663**: POS DELIVER-LATER DOOR (WH_POS_PICK_LANE W-1, SPATIAL_PICKING_SPEC §S-2b) — pos_lens.js?v=5 payment panel gains 'Deliver later · pick at warehouse' (#pos-float-deliverlater), DICTIONARY-GATED on a docsubtypeso='SO' doctype in the loaded db (seed 132); rides POSCore.buildDeliverLaterGroup VERBATIM (W-POS-DELIVERLATER: order CO + shipment born DR, invoice timing named from C_Order.InvoiceRule) then PERSISTS the op log via the new host persist (idempiere.html → KanbanHost.persist(opDb, idmp_kanban_proj)) so the warehouse walk can fold the sale — the §S-2b seam. pos_core.js?v=3 synced to the bim-compiler engine (gains deliverLaterPolicy/buildDeliverLaterGroup/completeShipmentOps); NEW erp/inout_confirm.js (W-WH-CONFIRM fold, lazy-fetched by the viewer walk, not precached). Tender path byte-identical (W-POS-LIVE falsifier). §POS-DELIVERLATER door=on/off + sale lines. PRIOR —
- **v660**: POS float-panel dispose-with-cart (§POS-FLOAT dispose=cart-empty — sale complete / last line removed closes the panel; ✕ + swipe-down landed v658; pos_lens ?v=4) + viewer PWA-resume dead-link recovery — OCI-era pwa_last_db bricked bare viewer opens; clears the key, ONE redirect to the landing (viewer sw v647, main.js?v=41).
- **v659**: NINJA EXCEL pill (NinjaExcelAdaptation.md §6.2, W-NINJA-PILL) — 'Excel Report' lens on idempiere.html: NEW ninja_excel.js (engine, == bim-compiler build/erp, headless-proven W-NINJA/W-NINJA-MAKE/W-NINJA-RULE maxDiff=0c) + ninja_pill.js (drop/download UI; MAKE scaffolds from a filled BACKUP sample, RUN folds Process-sheet SQL over the loaded tenant db via sql.js + verify-by-example in integer cents) + xlsx.mini.min.js (SheetJS, vendored, LAZY-loaded on first open) + ninja_sample.xlsx (GardenWorld invoice summary generated from ad_seed.db STORED columns — runnable as-is). pills_idmp.json +ninja (grid glyph, order 4.8); idempiere.html wires scripts + IdmpPillActions.ninja.
- **v658**: CONSISTENCY FINISH (prompts/CONSISTENCY_FINISH.md §K-1..§K-4, the 2026-06-12 cross-lane review's finish items) — NEW precached overlay_kit.js (window.OverlayKit: el/ensureSql/ico — the utilities erp_picker.js + migrate_showme.js each carried byte-identical, plus the ONE Lucide icon renderer over icons.js; ensureSql reuses the page's window.SQL, one WASM init per page); erp_picker.js?v=28/3 + migrate_showme.js?v=23 consume the kit and drop EVERY unicode glyph (brand 🟧🟣🔵🔴🟦→CSS color dots, ⬇→download, 🔌→plug, 🔒→lock, ✓/✗→check/xmark, ⧉→clipboard, &larr;/→→chevronLeft/next, ●→CSS livedot, step-done ::after ✓→data-URI check) per feedback_pill_icon_consistency; icons.js?v=26/5 +check/xmark/lock/plug/chevronLeft (verbatim Lucide, circleHelp precedent; idempiere.html loads icons+kit BEFORE migrate_showme — its STEPS render icons at module load). §K-3: installShard()'s persisted success-path records a World-history dot (WholeHistory.record page=idempiere kind=op 'Installed <shard> (N rows, clients a→b)') — the largest user state change was the only one leaving no W trace; guarded, NOT a signed op (install=infrastructure, outside the Z fold lane). §K-4b crud_overlay.js?v=3: crudFoldBack/crudFoldForward now RETURN Promise<boolean> (ok=Y/N appended to §FOLD-BACK/§FOLD-FORWARD; sidecar is async so a sync bool would be invented). §K-4c idmp_history.js?v=8: warn-once §IDMP-HIST no-restoreFn when the host never registerRestore()d (was: silent display-only bar). Witnesses: W-OVERLAY-KIT (poc_overlay_kit.js: kit consumed, zero glyphs in rendered ep-/ms- DOM) + W-INSTALL-DOT (poc_install_histdot.js: dialog install → §WHOLE-REC) + regressions poc_install_persist/poc_install_idmp/poc_client_switcher green.
- **v655**: POS FULL LOOP §L-1..§L-3 (POS_FULL_LOOP.md DONE) — replenish-PO-on-tap wiring in pos_lens.js: vendorOf() from m_product_po.iscurrentvendor=Y (honest refusal when absent), tap → buildReplenishPO → commitGroup (DR state); §POS-REPLENISH-PO log on tap; W-POS-CRUD/W-POS-VOID/W-POS-REPLENISH-LOOP all green headless. newVerbs=[]. Loop closed: suggest→PO CO→receipt CO→onhand +11→suggestion clears.
- **v657**: G-2 sha256 imageKey (POS_GAP_CLOSE) — pos_lens Import photo key = 'sha256:'+hex(SubtleCrypto digest of the DECODED bytes), replacing the content-length stub (live wiring now matches the W-IMG-SYNC content-addressing proof); img_store.resolveImage gains digestOf verification (tampered folder blob → §IMG-TAMPER, thumb keeps rendering); read path in _addNewTileCard rides resolveImage. Witness W-IMG-LIVE (poc_img_live.js): live-page IDB put/get round-trip + tier walk full→thumb→none + tamper falsifier. ?v= bumps: img_store 1→2, pos_lens 2→3.
- **v655**: POS FULL LOOP §L-1..§L-3 (POS_FULL_LOOP.md DONE) — replenish-PO-on-tap wiring in pos_lens.js: vendorOf() from m_product_po.iscurrentvendor=Y (honest refusal when absent), tap → buildReplenishPO → commitGroup (DR state); §POS-REPLENISH-PO log on tap; W-POS-CRUD/W-POS-VOID/W-POS-REPLENISH-LOOP all green headless. newVerbs=[]. Loop closed: suggest→PO CO→receipt CO→onhand +11→suggestion clears.
